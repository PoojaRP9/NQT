#include <bits/stdc++.h>
using namespace std;

int check (string str){
    int n=sizeof(str)/sizeof(str[0]);

    for(int i=0; i<n; i++){
        if(str[i]==)
    }
}

int main(){
    string str = "Hello, World!";

}